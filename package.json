{
  "name": "golf-maintenance-portal",
  "version": "1.0.0",
  "description": "Golf Course Maintenance Reporting System",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "cd frontend && npm install && npm run build",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 